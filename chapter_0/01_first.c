#include<stdio.h> // This is pre-processor directive 
// Pre-processor Directive lets use functions like print used here
// Pre-processor directive tells the compiler that before running the main function
// There are some Functions to used which needs to be included 
// To run main function

int main(){
    printf("Hello World");
    return 0;
}