#include<stdio.h>

int main(){
    int number; // this is stored in RAM 
    number = 6;
    printf("The Value of the number -> %d",number);

    // Characters in C 
    char a ;
    a = 's';
    return 0;
}