// variable simply the name of a memory location

#include <stdio.h>

int main(){
    int i = 10;
    int j = i;
    // %d is for int %f is for float %c is for character
    printf("The value of i is %d and value of j is %d",i,j);
    
    return 0;
}